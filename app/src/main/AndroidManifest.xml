<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppElite"
        tools:targetApi="31">

        <activity android:name=".RegistrarCliente" android:exported="false" />
        <activity android:name=".ReportesActivity" android:exported="false" />
        <activity android:name=".SaldosActivity" android:exported="false" />
        <activity android:name=".PagosActivity" android:exported="false" />
        <activity android:name=".VentasActivity" android:exported="false" />
        <activity android:name=".ProductosActivity" android:exported="false" />
        <activity android:name=".Pantalla_inicio" android:exported="false" />
        <activity android:name=".PantallaInicioModernaActivity" android:exported="false" />
        <activity android:name=".Register" android:exported="false" />

        <!-- 🔹 Faltaban estas -->
        <activity android:name=".ServiciosActivity" android:exported="false" />
        <activity android:name=".CotizacionesActivity" android:exported="false" />

        <!-- Splash Activity como actividad principal -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity (Login) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>
</manifest>

<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo gris claro -->
    <solid android:color="#F1F4F8" />

    <!-- Borde sutil -->
    <stroke
        android:width="1.2dp"
        android:color="#D5DBE5" />

    <!-- Esquinas bien ovaladas -->
    <corners android:radius="40dp" />

    <!-- Padding para hacerlo más alto -->
    <padding
        android:left="12dp"
        android:top="10dp"
        android:right="12dp"
        android:bottom="10dp" />
</shape>
